  
language: "node_js"
node_js:
  - "8"
  
cache: yarn

before_script:
  - yarn install
script: 
  - yarn test
  - yarn build
deploy:
  provider: heroku
  app: test-github-ci-react
  api_key: $HEROKU_API_KEY
  on:
    all_branches: true